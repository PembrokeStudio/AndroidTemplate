package pembroke.studio.lib.rx

/** Returns the inputs "ANDed" together. */
fun and(lhs: Boolean, rhs: Boolean) = lhs and rhs

/** Returns whether or not the value is true. */
fun isTrue(b: Boolean) = b